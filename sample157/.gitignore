# node_modules はサイズが非常に大きい node_modules には数千ものファイルが含まれることがあり、リポジトリの容量が爆発的に増えます。
# 再現性がある package.json と package-lock.json があれば、npm install や yarn install で同じ環境を再構築できます。つまり、コードだけ管理しておけば十分。
# # 依存関係は外部のもの 自分で書いたコードではなく、外部ライブラリの集合体なので、Git で追跡する必要はありません。
# プラットフォーム依存の可能性 OS や Node.js のバージョンによってインストールされる内容が微妙に変わることがあるため、Git に含めると逆にトラブルの元になります。

# dist はビルド成果物なので Git 管理外にする

/node_modules/
/dist/
node_modules
